import React, {Component} from "react";
import TodayDaily from './todayDaily';

const App = React.createClass({

    render() {
        return (
            <div>
                <TodayDaily/>
            </div>
        )
    }
});

export default App;
